# Tier-3 replay artifacts — pre-swap (`prior_v1`) at v2 code, SIMULATED

A-007 deliverable regenerated against the `freeze_prior_v2` dataset: one
replayable artifact per outcome class the v2 DOE actually produced —
**success, IS8-1, IS8-2** (Tier-1 marginals) and **IS8-5** (gate cell) —
rendered from logged trial state only (D-007: no imagery) by
`wyzantium_sim.replay`.

**Class-set delta vs `tier3_prior_v1`, and why it is evidence:** IS8-3 is
gone (v1's 17 Tier-1 IS8-3s were attempt-seam timer artifacts; D-042
removed them) and IS8-5 is new (ambiguity refusals exist only because
D-043 gave lone tags realistic flip behavior). The v1 set stays committed
— the ROADMAP protocol keeps both sides of every change.

Every GIF carries its trial_id; every sidecar carries the F-024
regeneration recipe (plan expression + tag + trial_id with embedded seed
— never a scratch path). Records regenerate deterministically from the
committed plans; these four were regenerated locally on the M4
(byte-identity is a per-instance-class contract, R01 F-018 — the freeze
ran on c7i.8xlarge; trial outcomes and trajectories reproduce, record
bytes need not). Regeneration SHA is a descendant of the freeze SHA
`f6325bd` whose only sim-runtime delta is the additive
`run_sweep(curve_sets=...)` swap seam, default-inert for `prior_v1`
(pinned by `sim/tests/test_swap_seam.py`).

Producer notes (probed 2026-08-14): IS8-2 comes from
`tag_knockout_mask=1` — the outer tag itself destroyed (the lux=1 cell
classifies IS8-1: floor detections keep ID-0 sightings nonzero). IS8-5's
first gate producer is `gate:00051`.

Regenerate this whole set: `sim/.venv/bin/python tools/tier3_v2.py`
Re-render any single record:
`sim/.venv/bin/python -m wyzantium_sim.replay render <record> --out <dir>`

Post-swap, the `mr_v1` artifact set is regenerated by the same driver in
the swap session and both sets are kept.
