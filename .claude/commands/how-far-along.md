---
description: One-paragraph plain-language status of what's built vs. the whole project
---

Answer the question "how far along are we?" like this:

1. Read `HOW-FAR-ALONG.md` at the repo root.
2. Check `git log --oneline -15` for anything newer than the file's
   "Last updated" line (new T-tasks, phase/gate changes). If stale, update the
   paragraph and its date, keeping the file's format rules (one short
   paragraph, high-school reading level; ROADMAP.md and PHASE1_PLAN.md §4 are
   the sources of truth for phase/task position).
3. Reply with just that paragraph — super concise, easy to read, no headers,
   no tables, no jargon. It must say what has been built so far relative to
   the rest of the project.
